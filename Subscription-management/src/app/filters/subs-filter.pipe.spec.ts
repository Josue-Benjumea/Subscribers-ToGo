import { SubsFilterPipe } from './subs-filter.pipe';

describe('SubsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SubsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
